---
phase: quick
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/onboarding/setup-wizard.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can click the Next button to proceed through wizard"
    - "User can copy the extraction command to clipboard"
    - "User can change language to see localized extraction commands"
  artifacts:
    - path: "src/components/onboarding/setup-wizard.tsx"
      provides: "Working wizard with all interactive elements"
      min_lines: 280
  key_links: []
---

<objective>
Fix get started flow: unable to click Next button, change languages, or copy code in first section

Purpose: Remove UI blockers in the onboarding wizard
Output: Working interactive wizard with Next, copy, and language functionality
</objective>

<execution_context>
@/home/aspharix/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aspharix/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/onboarding/setup-wizard.tsx
</context>

<tasks>

<task type="auto">
  <name>Investigate and fix setup wizard interactivity issues</name>
  <files>src/components/onboarding/setup-wizard.tsx</files>
  <action>
1. **Next button not clickable**: Check if button has disabled prop or validation blocking it. The Next button at line 249 should work - ensure it's properly enabled and has no overlay.

2. **Copy code button not working**: The copy button (line 135-142) uses `opacity-0 group-hover:opacity-100` which makes it invisible. Add a visible clickable state - make the copy icon always visible or at least visible on hover with proper pointer-events. Also add error handling for clipboard API failure.

3. **Language selector missing**: Add a language dropdown to step 1 that updates the extraction commands and UI labels. Use a small dropdown component or simple select element.

4. **Test all fixes**: Verify Next navigates, copy works, language switch changes commands.
  </action>
  <verify>
Build passes: npm run build 2>&1 | head -30
  </verify>
  <done>User can click Next, copy extraction commands, and select language in step 1</done>
</task>

</tasks>

<verification>
- Build succeeds
- Component renders without console errors
- All buttons are interactive
</verification>

<success_criteria>
User can complete step 1 of the wizard: select database type, copy command, change language, and proceed to Next
</success_criteria>

<output>
After completion, create .planning/quick/260327-cv1-fix-get-started-flow-unable-to-click-nex/260327-cv1-SUMMARY.md
</output>